# Save this file as conf.yaml
status: 'Techy is awesome!'
automatic: true  # Set if URLs matching the sites will have embeds automatically sent 
mention: false # Mention author on reply 
sites:
  - name: 'Twitter'
    active: true
    catch:
      - 'twitter.com'
  - name: 'Reddit'
    active: true
    catch:
      - 'reddit.com'
secrets:
  discord:
    token: 'your_discord_bot_token'